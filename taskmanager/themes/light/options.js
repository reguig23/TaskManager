import createComponents from '@/themes/light/createComponents'
import palette from '@/themes/light/palette'
import shadows from '@/themes/light/shadows'

const options = {
  components: createComponents(palette),
  palette,
  shadows,
}

export default options

import createComponents from '@/themes/dark/createComponents'
import palette from '@/themes/dark/palette'
import shadows from '@/themes/dark/shadows'

const options = {
  components: createComponents(palette),
  palette,
  shadows,
}

export default options
